---
tosca_definitions_version: tosca_simple_yaml_1_3

description: Service for LED ON and OFF

node_types:
  fog.nodes.ActuationService:
    derived_from: tosca.nodes.SoftwareComponent
    
    properties:
      actuators:
        type: list
        description: List of IP address of actuators.
    
    interfaces:
      Standard:
        type: tosca.interfaces.node.lifecycle.Standard
        inputs:
          actuators: { value: { get_property: [ SELF, actuators ] }, type: list }
        operations:
          create: playbooks/create.yaml
          delete: playbooks/delete.yaml

...